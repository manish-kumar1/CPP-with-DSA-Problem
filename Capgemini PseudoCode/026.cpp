
#include<iostream>
using namespace std;

int main(){

    // int p = 7, q = 8, r = 4;
    // if((p+q)<(10-p)){
    //     q = 8 + q;
    // }
    // if((q + r + p) > (p + q))
    //     p = (p + 12) + q;
    // q = (2 + 5) + q;

    // cout<< p+q+r;
    int i = 0;
    cout << (1 << i);

    return 0;
}